# コントリビューションガイド

本リポジトリは、株式会社ロジレスが提供する **LOGILESS API** の**非公式**ドキュメントです。公式ドキュメント（<https://app2.logiless.com/developer/>）をもとに整理・補足したものであり、ロジレス社が公式に提供・保証するものではありません。

修正・追記・誤りの指摘を歓迎します。小さな typo の修正から、新しいインターフェースの追加まで、どのような貢献も歓迎です。

## 貢献の流れ

1. **Issue を立てる**
   - 改善提案・誤りの指摘・質問などは、まず Issue を作成してください。
   - 公式ドキュメントの更新を本リポジトリへ反映する場合は、Issue テンプレート「**ドキュメント更新**」を利用してください。
2. **作業ブランチを作成する**
   - `main` から作業用のブランチを切ってください（例: `update/sales-return`、`fix/typo-article`）。
3. **変更して Pull Request を出す**
   - 変更を加えたら PR を作成してください。
   - PR の説明には、**参照した公式ドキュメントの該当箇所**（URL・スラッグなど）を必ず記載してください。レビュー時に原典との照合ができるようにするためです。

## 仕様の単一ソース (spec/*.json)

本リポジトリにおける API 仕様の**単一ソース（Single Source of Truth）**は、`spec/` 配下の **OpenAPI 3.0 形式（JSON）**ファイルです。

- インターフェース 1 リソースにつき 1 ファイルとします（例: `spec/sales_return.json`、`spec/article.json`）。
- 今後の仕様の**追加・修正・公式更新の反映は、まず `spec/*.json` を編集**してください。
- `docs/` 配下の Markdown は**人間向けの参照用**ドキュメントです。`spec` を変更した場合は、対応する Markdown も必ず整合するように更新してください。
- 公式は機械可読な仕様（OpenAPI 等）を提供していないため、`spec/` は**本リポジトリで独自に整備したもの**です。フィールド名・値・型などは公式の記載に基づいて作成・維持してください。

## ドキュメント記法のルール

既存の `docs/` の慣習にそろえてください。

### インターフェース文書の構成

以下の見出し構成に従います。

- `# 日本語名`（例: `# 売上返品`）
- `## プロパティ`（プロパティ表）
  - サブオブジェクトは `### line`、`### article` のように小見出しで表を分ける。
- `## エンドポイント`
  - 各操作の中で `#### リクエスト` / `#### レスポンス` を用いる。

### プロパティ表

- 表は `| プロパティ | 説明 |` の **2 列**とします。

  ```markdown
  | プロパティ | 説明 |
  | --- | --- |
  | id | [読取専用] 売上返品をユニークに識別するための数値です。 |
  ```

- **読取専用**のプロパティは、説明の冒頭に `[読取専用]` を付けます。
- **必須**のプロパティは、説明の冒頭に `[必須]` を付けます。

### 列挙型へのリンク

列挙型（ステータス・区分など）へのリンクは `../document_status/<slug>.md` の形式を用います。

```markdown
| tax_indicator | [税区分](../document_status/tax_indicator.md) |
```

### 日時・日付の表記

- 日時は `Y-m-d H:i:s` 形式と明記します（例: `2018-01-01 23:59:59`）。
- 日付は `Y-m-d` 形式と明記します（例: `2018-01-01`）。

### エンドポイント早見表の更新

新規エンドポイントを追加した場合は、`README.md` の「**エンドポイント早見表**」にも 1 行追加してください。

## 公式仕様の更新を反映する手順

1. 公式の <https://app2.logiless.com/developer/> でドキュメント一覧を確認します。
2. `README.md` の「**最終確認日**」と、既知のドキュメント一覧（`.github/known-documents.txt`）と比較して、差分を洗い出します。
3. 該当する `spec/*.json` と Markdown に反映し、あわせて `README.md` の**エンドポイント早見表**・**更新履歴**・**最終確認日**を更新します。
4. `.github/known-documents.txt` を更新します（新たに把握したドキュメントのスラッグを追記）。
5. **フィールド名・値は必ず公式の記載どおり**にします。推測で補完しないでください。不明な点は Issue / PR で明示してください。

## 更新検知の自動化

公式ドキュメントの更新検知は、GitHub Actions（`.github/workflows/check-official-updates.yml`）でも補助しています。公式ドキュメント一覧に**未知のドキュメントが出現すると、確認用の Issue が自動作成**されます。Issue が作成されたら、上記「公式仕様の更新を反映する手順」に従って対応してください。

## 著作権について

本ドキュメントの著作権は **株式会社ロジレス** に帰属します。本リポジトリ独自のライセンス（CC BY 等）は付与しません。コントリビューションを行う場合は、この前提に同意したものとみなします。
